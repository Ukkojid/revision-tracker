import React from 'react';

const Sync = () => {
  return (
    <div className="sync">
      <h1>Cross-Device Sync</h1>
      <p>Access your revision schedule across all devices.</p>
    </div>
  );
};

export default Sync;